# Tallivity — Master Implementation Checkpoint Table
**v3 · Generated 2026-04-21 · Source: review_1.md + tracker_v2.md**
*All existing rows carried forward. New rows added. C4 flagged for rework. Status reflects honest current state.*

---

## Legend
`todo` · `in-progress` · `needs-rework` · `done` · `blocked`

---

## Critical

| ID | Scope | Files | Commit message | Full intent & reasoning | Acceptance criteria | Status |
|---|---|---|---|---|---|---|
| C0 | Setup ledger | `specs/implementation_tracker_v3.md` | `chore(tracking): initialise v3 master checkpoint ledger` | Replaces v2. Single source of truth for all open, in-progress, and closed work. Prevents duplicate effort and gives QA a verified checklist. | File exists, all rows present, evidence column populated on close | done |
| C1 | VAD silence detection | `SpeechEngine.swift`, `SessionEngine.swift` | `feat(vad): integrate energy-based silence stop with per-phase thresholds` | Silence end-of-utterance is implemented using `currentInputLevel` metering with a 1.0s threshold. This is done but the threshold value is independently flagged as too aggressive (see C8). C1 covers wiring the mechanism; C8 covers calibrating it. Current state: mechanism works, values wrong. | Recording auto-stops after sustained silence; `recordingStopped = true` fires cleanly; no tap required | done — threshold values need C8 fix |
| C2 | Round-end recovery buffer | `SessionEngine.swift` | `feat(session): add deterministic 2s buffer before QA prompt fires` | After the work timer ends, the brain needs a minimal deceleration window before being asked reflective questions. A 2-second pause prevents the reflection phase from feeling like an immediate interrogation, which suppresses answer quality. The `try? await Task.sleep(for: .seconds(2))` in `runRoundEnd()` covers this. | 2s pause is observable between timer completion and first spoken QA prompt | done |
| C3 | Low-motivation starter block | `SessionEngine.swift` | `feat(session): add 5-min starter session path for motivation ≤ 2` | Behavioral activation theory: reducing task initiation cost is more effective than effort-based approaches at low motivational arousal. A 5-minute session lowers the commitment threshold to near-zero, producing momentum. The continuation decision after the starter is the key leverage point. | Motivation ≤ 2 → 5-min timer → spoken continuation prompt → yes continues full session, no exits cleanly | done |
| C4 | Flow extension | `SessionEngine.swift` | `fix(session): replace voice-activated flow extension with tap-based skip-break mechanism` | **Needs rework.** Current implementation listens for "keep going" / "extend" verbally with a 2.2s window at round end. This is directly counterproductive: interrupting with a verbal prompt requiring a timed spoken response is exactly the kind of attentional interrupt that terminates flow states (Csikszentmihalyi, 1990). The mechanism should be passive — a tap or button that extends by skipping the break, not an active voice command during the transition. The 2.2s listen window also produces false negatives constantly since users in flow are not expecting to speak. | Round-end UI shows a silent "Continue →" button. Tapping skips break and resumes next loop immediately. No audio prompt. No listen window. The `waitForFlowExtensionDecision()` function is removed entirely. | needs-rework |
| C5 | Gemma output batching | `GemmaEngine.swift` | `perf(gemma): batch streaming output at 200ms publish cadence` | Without batching, every token triggers a SwiftUI re-render cascade. At 10–20 tokens/second on a mobile VLM, this produces significant UI jank and thermal load. The 200ms publish buffer (`lastPublish` + `publishBuffer`) keeps rendering smooth. Final flush ensures no tail tokens are lost. | UI remains smooth during generation; no dropped tokens at stream end; thermal profile acceptable | done |
| C6 | Q&A question generation prompt | `GemmaPrompts.swift` | `fix(prompts): remove verbatim output instruction that defeats goal-specific Q&A generation` | Critical logic error: `generateQuestions()` instructs Gemma to output three example lines "verbatim" after asking it to generate goal-specific questions. This means every session receives identical generic questions regardless of the user's goal. Retrieval practice efficacy scales directly with question specificity (Roediger & Karpicke, 2006) — generic prompts produce generic answers, which are pedagogically worthless. Fix: remove the three hardcoded example lines and the "Output only those three lines verbatim" instruction. The fallback `presets(for: "default_qa_questions")` already handles generation failure gracefully. | Goal "finish the API docs" → questions reference API docs specifically. Goal "record guitar demo" → questions reference recording. Verified with 3 different goals on-device. | todo |
| C7 | Long break position | `SessionEngine.swift` | `fix(session): move long break to post-final-loop, not pre-final-loop` | Current logic: `completedLoops.count >= max(1, totalLoops - 1)` triggers long break before the last loop. For 4 loops, this fires after loop 3, giving the restorative long break immediately before the final push. This inverts the intent entirely. Attention Restoration Theory (Kaplan, 1995) places extended recovery after the effort set is complete, not before its final element. The user needs short breaks between work segments and a full recovery break after completing all planned work. Fix: long break only triggers when `completedLoops.count >= totalLoops`, i.e., after the session is fully done — or implement it as a post-session cooldown rather than an inter-loop break. | 4-loop session: loops 1–3 → 5-min breaks. Loop 4 completes → long break (or session ends). No long break fires before loop 4. | todo |
| C8 | Silence detection threshold calibration | `SessionEngine.swift` | `fix(vad): raise silence cutoff to 2.5s default, 3.0s during QA phases` | The 1.0s silence threshold in `listen()` is too short for reflective speech. Cognitive pause during deliberate verbal recall is 1.5–3.0s (Goldman-Eisler, 1968). Users answering "what got in your way?" need processing time before speaking. A 1-second cutoff will consistently cut off users mid-thought during Q&A, training shallow fast answers. Two calibrated values needed: `goalCaptureSilenceThreshold = 2.0s` (shorter utterance expected) and `qaSilenceThreshold = 3.0s` (deliberate reflection expected). Also: `silenceLevel = 0.01` should be reviewed — this is extremely sensitive and will fire on background noise as "silence". Suggest `0.03–0.05` as a more robust floor. | Goal capture: 2.0s pause before stop. Q&A answers: 3.0s pause before stop. Test in noisy environment (kitchen, café) that `silenceLevel` floor does not false-positive. | todo |
| C9 | Break minimum floor | `FocusView.swift`, `SessionEngine.swift` | `fix(ui): enforce 5-minute minimum break duration with explanatory label` | `RotaryTimePicker` currently allows break selection from 1 minute. Sub-5-minute breaks produce no measurable cognitive recovery (Ariga & Lleras, 2011). They create false confidence that rest has occurred while attention remains depleted. This is one of the most common user-behavior mistakes in Pomodoro usage and the app should not permit it. Fix: change `stride(from: 1, through: 30, by: 1)` to `stride(from: 5, through: 30, by: 1)`. Add a subtitle beneath the break picker: "min 5 min for actual recovery". | Break picker minimum value is 5. Values 1–4 are not selectable. Label visible in idle setup view. | todo |

---

## High

| ID | Scope | Files | Commit message | Full intent & reasoning | Acceptance criteria | Status |
|---|---|---|---|---|---|---|
| H1 | Crash recovery checkpoints | `SessionStore.swift`, `SessionEngine.swift`, `FocusView.swift` | `feat(session): add phase checkpoint persistence and silent resume` | Sessions can be interrupted by calls, lock screen, or app termination. Without persistence, the user loses all loop data and must restart. The checkpoint encodes phase, loop number, answers, goal, and durations. Resume re-enters at the nearest safe phase (work or break entry point). Currently implemented as silent auto-resume — flagged for UX improvement in M10. | Force-close mid-loop 2 → relaunch → resumes at correct loop with correct goal and prior loop data intact | done — M10 adds explicit confirmation prompt |
| H2 | Name ceiling in prompts | `GemmaPrompts.swift` | `fix(prompts): enforce single name appearance rule across all prompt templates` | Repeated name usage in Gemma outputs sounds robotic and is the most reliable signal that output is AI-generated rather than coach-genuine. All prompt templates now include "use the user's name exactly once, only at the start" or "name may appear at most once." This is a soft constraint — model compliance is not guaranteed — but the instruction significantly reduces frequency. The closing sentence pattern enforces structural name placement. | Closing sentence contains user name once. Memory recall output contains name zero or one times. Verified across 5 generated outputs per prompt type. | done — model occasionally violates, acceptable |
| H3 | Photo factual delta feedback | `GemmaPrompts.swift`, `SessionEngine.swift` | `feat(photo): integrate baseline-delta VLM comparison with factual sentence output` | Photo before and after a work session provides an embodied accountability signal. The VLM describes each photo factually, then a second prompt generates a single comparative sentence or outputs SKIP if evidence is insufficient. This avoids motivational language ("great progress!") and sticks to observable facts ("a second browser tab is now open alongside the document"). The `factualPhotoDelta` prompt enforces this. | Baseline + delta photos → factual comparative sentence spoken at round end. Sentence contains no praise language. SKIP fires when photos are too similar. | done — depends on E2B model image quality |
| H4 | Screen awake during work | `SessionEngine.swift` | `fix(timer): scope idle timer disable strictly to active work phases` | Screen dimming during a work session is a significant UX failure — the user cannot see the timer ring. `UIApplication.shared.isIdleTimerDisabled` must be enabled on work phase entry and disabled on every exit path: cancel, skip, session end, and crash. Currently implemented with `updateScreenAwake()` called at phase entry/exit. | Screen stays on for full 25-min work phase. Screen sleep resumes on cancel and at session report. Verified on device with auto-lock set to 30s. | done |
| H5 | Semantic session retrieval | `SessionStore.swift` | `feat(memory): add trigram cosine similarity for prior session retrieval` | Exact word match for memory recall is too brittle — a user working on "API documentation" will not match a prior session tagged "write API docs" without semantic overlap. Trigram decomposition + cosine similarity provides a lightweight vocabulary-independent similarity signal without requiring an embedding model. Threshold 0.18 is noted as potentially low in L1. | "finish the landing page copy" retrieves prior session tagged "copywriting for site launch" with score > 0.18. Unrelated goal "cook dinner" returns empty. | done — threshold review in L1 |
| H6 | Self-score behavioral anchors | `ScoreSelector.swift`, `prompts.json` | `fix(score): replace evaluative labels with behavioral output anchors` | Current labels (rough / below par / decent / solid / excellent) are subjective and emotionally loaded. Research on self-assessment (Kruger & Dunning, 1999) shows these labels produce inconsistent calibration and shame responses at low scores. Behavioral anchors tied to task completion are both more honest and more useful for downstream analysis. New anchors: 1 = did not start stated task · 2 = started, less than half done · 3 = meaningful progress made · 4 = task substantially complete · 5 = task complete, possibly exceeded. These map directly to the `LoopRecord.score` integer and make cross-session score comparisons meaningful. | Score UI shows behavioral anchor text beneath the circle row. Labels updated in `ScoreSelector`. Voice prompt references output not quality ("how much of the goal did you complete"). | todo |
| H7 | Motivation threshold justification and framing | `SessionEngine.swift`, `FocusView.swift`, `prompts.json` | `feat(motivation): add brief evidence-based framing for low-motivation path` | The threshold `motivation <= 2` for the starter block is functionally sound but arbitrary as currently implemented. The user receives no explanation of why the session is shortened. Adding one spoken line ("Low energy works better in short bursts — let's do 5 minutes and see") grounds the decision in physiology rather than app logic, which increases compliance and reduces abandonment. Additionally, motivation level 3 users currently get no differentiated path — consider whether 3 warrants a note about steady-state work style. | Level 1–2: spoken framing line before starter timer. Level 3: no change to flow. Line text stored in `prompts.json`. | todo |
| H8 | Break activity guidance | `SessionEngine.swift`, `prompts.json` | `fix(break): replace generic break recovery cue with specific physical action prompt` | `breakRecoveryPresets.randomElement()` delivers a random break line. Without specific guidance, the most common break behavior is checking a phone or email — activities with net-negative effects on attention restoration (Steidle & Werth, 2013). Walking, non-screen physical movement, and unfocused gaze at a distance are the three evidence-based restorative behaviors. The break start voice line must name a specific physical action, not a vague "rest" or "recharge" instruction. The ambient background visual correctly reinforces non-engagement, but the voice cue must close the gap. | Break start voice line contains at least one of: "walk", "look away from screen", "stand up", "look outside". Generic "take a break" lines removed from `break_start` presets. | todo |
| H9 | Cross-loop blocker pattern detection | `SessionEngine.swift`, `GemmaPrompts.swift` | `feat(session): detect repeated blocker patterns across loops and surface them` | Answers to "what got in your way?" are collected per loop but never cross-referenced. If a user names the same blocker in loops 2 and 3, they receive no signal that a pattern is forming. Pattern recognition is one of the highest-value pedagogical levers in spaced retrieval and self-regulation research (Zimmerman, 2002). After loop 2 onwards, compare the current blocker answer against prior loop answers using the same trigram similarity function in `SessionStore`. If similarity > 0.35 between two or more answers, inject a spoken line: "That blocker came up again — worth making a note of it." | Tested with identical blocker answers across loops 2+3: pattern detection fires, spoken line plays. Non-matching blockers produce no extra output. | todo |
| H10 | Q&A answers visible in session report | `SessionReportView.swift` | `feat(report): add collapsible per-loop Q&A answer section to session report` | `LoopRecord.answers` contains the three verbatim transcribed answers per loop. These are stored but never shown to the user. Reading your own words post-session is a direct metacognitive consolidation mechanism — seeing what you actually said forces comparison against what you intended to say. The report currently shows only scores, blocker summary, and closing sentence. A collapsible "Loop N" section per loop showing Q: and A: pairs adds the highest-density insight without cluttering the default view. | Session report shows collapsed "Loop 1 / Loop 2 / …" accordions. Tapping expands to show 3 Q+A pairs with question text and transcribed answer. Empty answer shows "—". | todo |
| H11 | Closing sentence output validation | `SessionEngine.swift`, `GemmaPrompts.swift` | `fix(prompts): add post-generation filter for praise-shaped closing sentence output` | The `closingSentence` prompt correctly instructs "no praise, no adjectives, no emotional framing." However, Gemma 4 E2B at 4-bit quantization has inconsistent instruction-following for stylistic constraints, particularly for positively-valenced output suppression. In practice, outputs like "Well done, Alex…" or "Great effort today…" will appear regularly. Unconditional praise after low-score sessions is actively demotivating for achievement-oriented users (Mueller & Dweck, 1998). Post-processing filter: strip leading phrases matching `^(Well done|Great job|Amazing|Fantastic|You did|Excellent work)[,\s]`. If output contains ≥ 2 exclamation marks, discard and use template fallback. | High-score session: closing sentence contains no "well done / great / amazing". Low-score session: factual sentence, no consolation framing. Tested with 5 generated outputs per motivation level. | todo |

---

## Medium

| ID | Scope | Files | Commit message | Full intent & reasoning | Acceptance criteria | Status |
|---|---|---|---|---|---|---|
| M1 | Configurable loop count | `FocusView.swift`, `SessionEngine.swift`, `SessionStore.swift` | `feat(session): make loop count configurable at session start` | Fixed loop counts prevent users from matching session length to their available time window. The rotary picker at idle setup lets users select 1–6 loops, and `totalLoops` is passed through the session engine and persisted in checkpoints. | Loop picker shown in idle view. Engine respects selected value. Checkpoint stores and restores it. | done |
| M2 | Break restoration UX | `FocusView.swift`, `SessionEngine.swift` | `feat(break): add ambient animated background and recovery voice cue` | A blank break screen gives no environmental signal that a qualitatively different phase has started. The animated gradient with floating blurred circles uses slow motion (14s) and desaturated colors to reduce arousal — this is deliberate contrast with the static white work screen. The motion is not stimulating; it is ambient. | Break phase shows animated gradient. Motion does not loop abruptly. Colors are desaturated. | done — H8 adds specific spoken guidance |
| M3 | Neutral score framing | `ScoreSelector.swift`, `SessionEngine.swift` | `fix(score): replace evaluative labels with output-based wording` | Evaluative labels ("rough", "below par") carry shame valence at low scores. Neutral output-based framing removes the emotional charge and focuses on task completion as the measure. This is a partial fix — H6 adds behavioral anchors on top of this. | Score labels updated to neutral output-based language. Spoken score prompt uses output framing. | done — H6 adds behavioral anchors |
| M4 | Pre-recorded fixed prompt scaffold | `SpeechEngine.swift`, `SessionEngine.swift` | `feat(audio): add cue-first fixed prompt system with TTS fallback` | TTS-synthesized voice has latency and quality variance. Pre-recorded cue files for fixed prompts (goal capture, round end, break start) give instant reliable delivery at the most critical session moments. `playCueAndWait()` + `sayFixed(cue:fallback:)` implements cue-first with graceful TTS fallback if asset is missing. | Fixed prompts use cue files when present. Missing cues fall through to TTS without error. | done — cue assets not fully populated |
| M5 | Circadian session time logging | `SessionStore.swift`, `SessionReportView.swift` | `feat(analytics): log session start time and surface time-of-day performance pattern` | Session `date` is already stored in `SessionArtifact`. Adding `startHour: Int` allows the app to compute whether the user's highest-scoring sessions cluster in the morning, midday, or afternoon. After 5+ sessions, surface a single factual line in the idle view: "Your best sessions start before 11am." This is passive awareness, not intervention — the user draws their own conclusion. Chronobiology (Monk, 2005; Kleitman, 1963) establishes reliable individual circadian performance peaks. | `SessionArtifact` gains `startHour` field. After 5 sessions, idle view shows time-of-day pattern line if variance is meaningful (std dev > 1.5h and top quartile clusters). | todo |
| M6 | Loop count upper warning | `FocusView.swift` | `feat(ui): show evidence-based advisory at 5–6 loop selection` | The rotary picker allows up to 6 × 25-min loops = 2.5 hours of focus time, which at standard breaks is 3.5 hours total. This exceeds the practical upper bound of sustained deliberate practice (Ericsson et al., 1993: elite performers average 4h/day, rarely in one block). No signal tells the user this. A one-line advisory beneath the loop picker at 5–6 selection: "Most people sustain deep focus for 2–4 hours. Consider splitting across two sessions." This is advisory, not a hard block. | At 5 or 6 loops selected, a secondary label appears below the loop picker. Label disappears at ≤ 4. | todo |
| M7 | Photo capture confirmation feedback | `FocusView.swift`, `SessionEngine.swift` | `fix(photo): add visual confirmation after baseline photo is accepted` | After `setBaselinePhoto()` is called, the sheet dismisses and the session continues. The user has no feedback that the photo was accepted and will be used. On a slow device the VLM description may take several seconds — during which silence feels like failure. A brief confirmation state ("Photo saved — starting session") or loading indicator closes this gap. | After baseline photo taken: sheet dismisses → brief "Photo saved" overlay or text → session proceeds. If VLM description is running, a subtle loading indicator is visible. | todo |
| M8 | Goal specificity gate | `SessionEngine.swift` | `fix(session): add minimum-word check and specificity re-prompt for goal capture` | A transcription artifact of "um" or a single word passes the current `!goal.isEmpty` gate. Vague goals ("do work") produce implementation intentions with no actionable specificity, which research shows to be no more effective than no goal at all (Gollwitzer, 1999). Gate: require minimum 4 words. If under-threshold, re-prompt once: "Can you say more about what you want to finish?" If second attempt also fails the gate, proceed with whatever was captured rather than blocking the user. | Single-word goal triggers one re-prompt. Two-word goal triggers one re-prompt. Four-word+ goal proceeds immediately. Second attempt accepted regardless of length. | todo |
| M9 | Whisper model recommendation for non-native speakers | `OnboardingView.swift`, `SettingsView.swift` | `feat(settings): add non-native speaker advisory for model selection` | The `small` default Whisper model has meaningfully higher word error rates for non-native English speakers, accented speech, and domain-specific vocabulary. Since goal text and Q&A answers are the primary data artifacts of the app, transcription accuracy directly affects output quality. Onboarding and settings should surface this: "If English isn't your first language, the Large model gives better accuracy." This is purely advisory — the user decides. | Onboarding model selection step shows advisory line. Settings speech model section shows same advisory. No automatic model switching. | todo |
| M10 | Explicit checkpoint resume prompt | `FocusView.swift`, `SessionEngine.swift` | `fix(session): replace silent auto-resume with user-confirmed resume modal` | `resumeSessionIfAvailable()` currently resumes silently on `.task` in `FocusView`. A user who closed the app intentionally, finished a call, or simply wants to restart receives no choice. They are dropped back into an in-progress session mid-loop with no context. The motivation level from hours ago may be completely wrong. Fix: show a modal or card in the idle view: "You have an active session — [goal summary]. Continue or start fresh?" Starting fresh clears the checkpoint. | Relaunch with active checkpoint: idle view shows resume prompt card with goal text. "Continue" resumes. "Start fresh" calls `store.clearCheckpoint()` and proceeds to idle. No auto-resume without confirmation. | todo |
| M11 | Visual state split: transcribing vs speaking | `SessionEngine.swift`, `FocusView.swift` | `fix(ui): separate isTranscribing and isSpeaking visual states` | `isProcessingSpeech` returns `true` for both `.transcribing` and `.speaking`. This causes the question text to dim and reduce weight during TTS playback — exactly when the user is reading the question while listening to it. The intent of dimming is to signal "the system is processing your input" (i.e., transcribing), not "the system is outputting audio." Fix: split into `isTranscribing: Bool` and `isSpeaking: Bool`. Apply dim/light-weight only during transcription. During TTS, question text remains full-weight and full-opacity. | During TTS playback of a question: question text is full opacity and `.semibold`. During transcription of user answer: transcript text is dimmed and `.regular`. | todo |
| M12 | Session history view | `SessionReportView.swift`, `SessionStore.swift` | `feat(history): add session history list accessible from idle view` | `SessionStore.loadAll()` returns all stored artifacts but no UI exposes them. Users cannot review patterns, past goals, or score trends without external tooling. A minimal history list (goal · date · avg score · loops) accessible via a secondary button in the idle view gives users longitudinal awareness without requiring a separate analytics screen. This is the minimum viable reflection surface for multi-week use. | History button in idle view opens a list of past artifacts. Tapping a row shows its `SessionReportView` in read-only mode. Minimum viable — no charts required at this stage. | todo |

---

## Low

| ID | Scope | Files | Commit message | Full intent & reasoning | Acceptance criteria | Status |
|---|---|---|---|---|---|---|
| L1 | Semantic retrieval threshold | `SessionStore.swift` | `fix(memory): raise cosine similarity threshold from 0.18 to 0.28` | 0.18 is low enough to match loosely related sessions and generate misleading memory recall. A user working on "tax filing" should not receive a recall about "write blog post" just because both contain common trigrams. 0.28 provides a tighter relevance signal without becoming so strict it never fires. Validate with a set of paired goals: related pairs (API docs / write documentation) should score > 0.28; unrelated pairs (tax filing / guitar practice) should score < 0.28. | Threshold updated. Regression: 5 related goal pairs still retrieve. 5 unrelated pairs return empty. | todo |
| L2 | Boot messages suppressed after onboarding | `RootView.swift` | `fix(ui): suppress whimsical boot messages post-onboarding` | Rotating messages like "Downloading a little patience…" are charming on first launch. On the 30th launch they are noise that extends perceived boot time and reduces trust. After `settings.onboardingComplete == true`, boot view should show only the app name and a minimal loading indicator — no rotating text. | First launch: rotating messages visible. Post-onboarding: boot shows only "Tallivity" + `BouncingDots`. | todo |
| L3 | Haptic generator preparation | `SessionEngine.swift` | `fix(haptic): call prepare() before first impact use` | `UIImpactFeedbackGenerator` has latency on first use if `prepare()` is not called in advance. The 5-minute haptic tick during long work sessions will occasionally fire with noticeable delay on the first occurrence. Calling `haptic.prepare()` at `workActive` phase entry pre-warms the generator. | First haptic tick at 5-min mark fires without perceptible delay. Verified on older device (iPhone 12 or earlier). | todo |
| L4 | Long break duration as configurable ratio | `FocusView.swift`, `SessionEngine.swift` | `fix(session): expose long break duration or remove fixed 4× multiplier` | `longBreakDuration = breakMinutes * 4 * 60` is a silent hardcoded ratio the user never sees. For a 5-min break selection this gives 20 min long break — reasonable. For a 15-min break selection this gives 60 min — likely unintended. Either expose a separate long break picker, cap the ratio (max 30 min), or make the calculation visible to the user. The hidden multiplication is a source of surprise. | Either: long break picker appears in idle setup. Or: maximum long break capped at 30 min with label "Long break: [N] min" shown. | todo |
| L5 | Negative score label framing | `ScoreSelector.swift` | `fix(score): soften score 1–2 label language` | "rough" and "below par" carry negative valence that can trigger shame or defensive response rather than honest reflection. Neutral alternatives: 1 = "didn't start" · 2 = "partial start" are more clinical and accurate. This is a refinement on top of H6 behavioral anchors — label language is the first visible signal, anchor descriptions are secondary. | Score 1 label: "didn't start" or equivalent. Score 2 label: "partial start". No negative evaluative language. | todo |
| L6 | Break ambient animation with short breaks | `FocusView.swift` | `fix(break): gate ambient animation duration to break length` | `breakAmbientShift` toggle triggers a 14-second animation loop that starts immediately on break phase appear. For a 5-minute break this is fine. For a theoretical very short break (edge case during testing), the animation loop creates visual noise during a phase that should feel calm. Also, if the break is skipped quickly, `onDisappear` with an active animation may produce a brief layout flicker. Gate the animation start with a 1-second delay after appear. | Animation starts 1s after break phase appears. Skipping break within 1s shows no animation. No flicker on rapid phase transition. | todo |
| L7 | `SettingsStore` instantiation in `finishSession` | `SessionEngine.swift` | `fix(session): remove SettingsStore instantiation inside finishSession()` | `finishSession()` instantiates a new `SettingsStore()` to get `userName`. This creates a second `UserDefaults`-backed object mid-session. `SessionEngine` already receives a `userName` parameter at session start and stores `currentGoal`. The name should be passed through and stored as a property at `startSession()` call time, not re-fetched at session end. This is a correctness issue: if settings change mid-session (unlikely but possible), the closing sentence would use the new name, not the session name. | `finishSession()` uses a `sessionUserName: String` stored property set at `startSession()`. No `SettingsStore()` instantiation inside `finishSession`. | todo |

---

## Release Gate

| ID | Scope | Files | Commit message | Full intent & reasoning | Acceptance criteria | Status |
|---|---|---|---|---|---|---|
| R1 | On-device full regression | all | `chore(release): on-device full regression sweep` | All Critical and High items must be closed before this gate passes. Full regression covers: 4-loop session start to report on device, all phase transitions, cancel at every phase, crash-resume round trip, low-motivation path, photo capture both phases, VAD silence in quiet and noisy environments, Gemma generation for all prompt types, TTS playback for all cue points, session history read. | All C + H rows: done. No crash in 3 consecutive full sessions on device. Session report populates correctly with Q&A answers visible. | in-progress — blocked on C4 rework + C6/C7/C8/C9 |

---

**Open critical path:** C4 (rework) → C6 → C7 → C8 → C9 must all close before R1 can pass. H6, H9, H10, H11 are the highest-leverage high-priority items after that.